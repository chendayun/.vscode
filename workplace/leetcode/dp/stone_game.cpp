#include <bits/stdc++.h>
using namespace std;
bool stoneGame(vector<int> &piles)
{
    int n = piles.size();
    int dp[n] = {0};
    for (int i = 0; i < count; i++)
    {
        /* code */
        if()
    }
}